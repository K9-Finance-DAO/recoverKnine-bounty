# UI (Vite) env vars. Place this file as ui/deploy/.env and edit values.

# WalletConnect Cloud project ID (required for WalletConnect connector)
VITE_WALLETCONNECT_PROJECT_ID=

# Enable local Hardhat chain in dev
# When true (or when running vite in dev mode), the app will include chain 31337
VITE_ENABLE_DEV_CHAIN=true

# Local Hardhat JSON-RPC URL (used when dev chain is enabled)
VITE_HARDHAT_RPC_URL=http://127.0.0.1:8545

# Note: Only variables prefixed with VITE_ are exposed to the UI.

# Optional: Enable an additional Hardhat chain using a different RPC (e.g., Cloudflare Tunnel)
# When true, the network switcher will show a second Hardhat option that uses the URL below
VITE_ENABLE_HARDHAT_TUNNEL=false
# RPC URL for the Hardhat (Tunnel) chain
VITE_HARDHAT_TUNNEL_RPC_URL=https://hardhat.example.com
# Optional: Display name for the tunnel chain
VITE_HARDHAT_TUNNEL_NAME=Hardhat (Tunnel)

# Optional: GitHub repo (owner/name) to link build hash to commit page
VITE_GITHUB_REPO=K9-Finance-DAO/recoverKnine-bounty

# Optional: Allowed hosts for HMR (comma-separated list)
ALLOWED_HOSTS=localhost,127.0.0.1
